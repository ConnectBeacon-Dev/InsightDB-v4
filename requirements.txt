# Company Intelligence Query Engine - Dependencies
# Install with: pip install -r requirements.txt

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Semantic Search (sentence-transformers)
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0

# LLM Support (Qwen integration)
llama-cpp-python>=0.2.0

# Web Interface (Flask app)
Flask>=2.3.0

# Optional: For faster CPU inference
# scikit-learn>=1.3.0

# Note: torch will download ~2GB on first install
# For offline installation, download wheels first:
# pip download -r requirements.txt -d packages/
# Then install offline: pip install --no-index --find-links=packages/ -r requirements.txt
